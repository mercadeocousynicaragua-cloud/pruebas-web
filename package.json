{
  "name": "cousy-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./docs/styles/tailwind.css -o ./docs/assets/app.css --minify",
    "build": "npm run build:css && node ./scripts/build.mjs",
    "qa": "npm run build && node ./scripts/qa.mjs",
    "export:docs": "npm run build",
    "build:pages": "npm run build",
    "dev": "node ./scripts/dev.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}

